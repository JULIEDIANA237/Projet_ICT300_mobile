/*import 'package:http/http.dart' as http;*/
// ignore: unused_import
import 'dart:convert';

// ignore: unused_import
import 'package:campus_shift/models/registration_model.dart';

class RegistrationController {
  /*Future<void> register(RegistrationModel registrationData) async {
    const url = 'https://your-laravel-api-url/register';

    final response = await http.post(
      Uri.parse(url),
      body: {
        'username': registrationData.username,
        'email': registrationData.email,
        'role': registrationData.role,
        'phone': registrationData.phone,
        'password': registrationData.password,
      },
    );

    if (response.statusCode == 200) {
      // L'inscription a réussi
      final responseData = json.decode(response.body);
      // Gérer la réponse du serveur si nécessaire
    } else {
      // L'inscription a échoué
      final errorData = json.decode(response.body);
      // Gérer l'erreur du serveur si nécessaire
    }
  }*/
}